# CRM — исправление polyPerimeterCm

Исправлена ошибка:
- polyPerimeterCm is not defined

Причина:
- функция расчета периметра окантовки с офсетом не попала в предыдущую сборку.

Теперь CRM снова подключается к Supabase и считает окантовку по контуру резки с офсетом.
